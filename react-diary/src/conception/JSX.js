const JSX = () => {
    return (
        <>
           <h2>JSX란 ? : JavaScript Extension , JS와 HTML을 합쳐서 사용할 수있는 문법 !</h2>
           <h2>Component ? </h2>
           <p>
             import , export : import로 불러오고, export default로 내보내기 가능 !<br/>
             component create :
             component import :
             component export :
             component adapt : 
           </p>

           <h2>JSX 규칙 ? </h2>
           <p>
            Tag : open , close tag 필수 ! (단! self-closing tag 사용 가능 !)<br/>
            최상위 tag : return의 최상위 태그는 하나 ! (단! 하기싫다면 React.Fragment tag 사용 !)
           </p> 
        </>
    );
}

export default JSX;